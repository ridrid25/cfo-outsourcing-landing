# Спецификация `?start=`-токена для @RidFinance_bot

Лендинг после прохождения экспресс-диагностики формирует ссылку

```
https://t.me/RidFinance_bot?start=<ТОКЕН>
```

Посетитель нажимает кнопку **«Отправить разбор Татьяне»** → открывается бот →
Telegram присылает боту `/start <ТОКЕН>` (deep-link параметр). Бот на Google
Apps Script **уже умеет принимать `?start=`**; задача доработки — **раскодировать
токен** по этой спецификации и развернуть разбор в таблицу/сообщение Татьяне.

> У сайта нет бэкенда. Все данные диагностики передаются только этим токеном.

---

## 1. Формат токена

```
d3_<ниша>_<зоны5>_<ответы3>_<оборот>_<приоритет>
```

* Разделитель частей — символ подчёркивания `_`.
* Первая часть `d3` — **версия формата** (текущая — 3). Если версия не `d3` —
  бот не знает раскладку, показывает сырой токен и просит уточнить вживую.
* Допустимые символы (ограничение Telegram deep-link): `A–Z a–z 0–9 _ -`.

| № | Часть | Длина | Что это |
|---|-------|-------|---------|
| 0 | `d3` | 2 | версия формата |
| 1 | `<ниша>` | 1 | ниша бизнеса (шаг 1), словарь 2.1 |
| 2 | `<зоны5>` | 5 | отмеченные зоны риска — битовая строка `0/1`, словарь 2.2 |
| 3 | `<ответы3>` | 3 | ответы на 3 поведенческих вопроса, словарь 2.3 |
| 4 | `<оборот>` | 1–2 | месячный оборот, млн ₽ (целое 5–80) |
| 5 | `<приоритет>` | 1 | индекс приоритетной зоны-диагноза `0–4` или `x`, словарь 2.4 |

---

## 2. Словари кодов

### 2.1. Ниша (часть 1) — 1 символ

Порядок — как на сайте.

| Код | Ниша |
|-----|------|
| `t` | Торговля |
| `p` | Производство |
| `u` | Услуги |
| `s` | Строительство |
| `m` | Маркетплейсы |
| `f` | Общепит |
| `o` | Другое |
| `x` | Не выбрана |

### 2.2. Зоны риска (часть 2) — битовая строка из 5 символов `0/1`

Порядок фиксированный. На позиции `1`, если зона отмечена на сайте.

| Поз. | Код зоны | Формулировка на сайте |
|------|----------|------------------------|
| 0 | `deadstock` | Мёртвый склад, зависшие остатки |
| 1 | `receivables` | Растёт дебиторка, платят с задержкой |
| 2 | `discounts` | Скидки «на глаз», без расчёта маржи |
| 3 | `profitability` | Не знаю прибыльность по продуктам |
| 4 | `cashgap` | Случаются кассовые разрывы |

Пример: `01001` → отмечены **дебиторка** (поз.1) и **кассовые разрывы** (поз.4).

### 2.3. Ответы на вопросы (часть 3) — 3 символа `q1 q2 q3`

Каждый символ: `0` / `1` / `2` — выбранный вариант, либо `-`, если вопрос
не отвечен. Чем выше балл — тем острее ситуация. (Это поддерживающий контекст
к диагнозу — показывает, почему зона стала приоритетной.)

| Поз. | Вопрос | Усиливает зону | `0` | `1` | `2` |
|------|--------|----------------|-----|-----|-----|
| q1 | Знаете, сколько денег будет на счетах через месяц? | `cashgap` | Вижу прогноз в любой момент | Прикидываю раз в месяц | Вспоминаю, когда прижмёт |
| q2 | Назовёте самый прибыльный продукт по цифрам за 5 сек? | `profitability` | Да, знаю точную маржу | Примерно чувствую | Не уверен |
| q3 | Скидки клиентам — как решаете? | `discounts` | По расчёту маржи | По ситуации | Как договоримся |

Пример: `2--` → q1 = «Вспоминаю, когда прижмёт» (балл 2), q2 и q3 не отвечены.

### 2.4. Приоритетный диагноз (часть 5) — индекс зоны

Сайт уже выбрал **главную зону** (по остроте, тай-брейк — сумма потерь) и
кладёт её индекс из таблицы 2.2 (`0–4`). Боту **не нужно** повторять эту
логику — берём зону по индексу.

| Индекс | Зона | Заголовок диагноза (для сообщения) |
|--------|------|------------------------------------|
| `0` | deadstock | Склад, который не двигается |
| `1` | receivables | Дебиторка: деньги есть, но не у вас |
| `2` | discounts | Скидки на ощущениях съедают маржу |
| `3` | profitability | Непонятно, что кормит, а что тянет вниз |
| `4` | cashgap | Деньги во времени — самая опасная зона |
| `x` | — | Острых зон не выявлено |

---

## 3. Пример: токен → расшифровка

Токен:

```
d3_s_01001_2--_30_1
```

| Часть | Значение | Расшифровка |
|-------|----------|-------------|
| версия | `d3` | формат 3 |
| ниша | `s` | Строительство |
| зоны | `01001` | Дебиторка + Кассовые разрывы |
| ответы | `2--` | q1 = «Вспоминаю, когда прижмёт»; q2, q3 — без ответа |
| оборот | `30` | 30 млн ₽/мес |
| приоритет | `1` | Дебиторка: деньги есть, но не у вас |

---

## 4. Шаблон сообщения для Татьяны

После расшифровки бот выводит (в чат и/или строкой в таблицу):

```
🧭 Новый разбор с сайта

Ниша: Строительство
Оборот: ~30 млн ₽/мес

Отмеченные зоны риска:
• Дебиторка, платят с задержкой
• Кассовые разрывы

Ответы:
• Деньги на счетах через месяц: «Вспоминаю, когда прижмёт»
• Прибыльный продукт по цифрам: — (не ответили)
• Скидки клиентам: — (не ответили)

Приоритетный диагноз: Дебиторка — деньги есть, но не у вас.

(токен: d3_s_01001_2--_30_1)
```

Пустые значения («не выбрана / не отвечено / острых зон нет») можно опускать
или показывать как «—».

---

## 5. Лимит 64 символа — обоснование

Максимально длинный токен:

```
d3 _ x _ 11111 _ 222 _ 80 _ 4
2  1 1 1   5   1  3  1  2 1 1   = 19 символов
```

| Часть | Макс. длина |
|-------|-------------|
| `d3` | 2 |
| `_` × 5 | 5 |
| ниша | 1 |
| зоны | 5 |
| ответы | 3 |
| оборот | 2 (макс «80») |
| приоритет | 1 |
| **Итого (максимум)** | **19** |

**19 ≤ 64** с большим запасом. Символы только из `A–Za–z0–9_-` → лимит
Telegram deep-link соблюдён при любом сочетании ответов.

---

## 6. Готовая функция расшифровки (Google Apps Script / JavaScript)

Вставляется в Apps Script как есть.

```js
var NICHE = { t:'Торговля', p:'Производство', u:'Услуги', s:'Строительство',
              m:'Маркетплейсы', f:'Общепит', o:'Другое', x:'Не выбрана' };

var ZONES = [
  { key:'deadstock',     chip:'Мёртвый склад, зависшие остатки',     diag:'Склад, который не двигается' },
  { key:'receivables',   chip:'Дебиторка, платят с задержкой',       diag:'Дебиторка: деньги есть, но не у вас' },
  { key:'discounts',     chip:'Скидки «на глаз», без расчёта маржи',  diag:'Скидки на ощущениях съедают маржу' },
  { key:'profitability', chip:'Не знаю прибыльность по продуктам',    diag:'Непонятно, что кормит, а что тянет вниз' },
  { key:'cashgap',       chip:'Кассовые разрывы',                     diag:'Деньги во времени — самая опасная зона' }
];

var QUESTIONS = [
  { q:'Деньги на счетах через месяц', opts:['Вижу прогноз в любой момент','Прикидываю раз в месяц','Вспоминаю, когда прижмёт'] },
  { q:'Прибыльный продукт по цифрам', opts:['Знаю точную маржу','Примерно чувствую','Не уверен'] },
  { q:'Скидки клиентам',              opts:['По расчёту маржи','По ситуации','Как договоримся'] }
];

// Раскодировать токен. Возвращает объект разбора либо null, если токен чужой/старый.
function decodeToken(token) {
  var p = String(token || '').split('_');
  if (p[0] !== 'd3' || p.length !== 6) return null;     // не наш формат
  var niche = p[1], mask = p[2], answers = p[3], turnover = p[4], priority = p[5];
  if (!/^[01]{5}$/.test(mask)) return null;

  var zones = [];
  for (var i = 0; i < 5; i++) if (mask.charAt(i) === '1') zones.push(ZONES[i].chip);

  var answersText = [];
  for (var j = 0; j < 3; j++) {
    var a = answers.charAt(j);
    answersText.push({
      q: QUESTIONS[j].q,
      answer: (a === '-' ? null : (QUESTIONS[j].opts[Number(a)] || null))
    });
  }

  var diagnosis = /^[0-4]$/.test(priority) ? ZONES[Number(priority)].diag : null;

  return {
    niche: NICHE[niche] || NICHE.x,
    turnover: Number(turnover),   // млн ₽/мес
    zones: zones,                 // массив отмеченных формулировок
    answers: answersText,         // [{q, answer|null}]
    diagnosis: diagnosis,         // строка диагноза или null
    raw: token
  };
}

// Собрать текст сообщения для Татьяны.
function renderForTatiana(token) {
  var d = decodeToken(token);
  if (!d) return 'Не удалось разобрать токен: ' + token;
  var L = ['🧭 Новый разбор с сайта', '',
    'Ниша: ' + d.niche,
    'Оборот: ~' + d.turnover + ' млн ₽/мес', '',
    'Отмеченные зоны риска:'];
  L = L.concat(d.zones.length ? d.zones.map(function (z) { return '• ' + z; }) : ['• не отмечены']);
  L.push('', 'Ответы:');
  L = L.concat(d.answers.map(function (a) { return '• ' + a.q + ': ' + (a.answer || '—'); }));
  L.push('', 'Приоритетный диагноз: ' + (d.diagnosis || 'острых зон не выявлено'),
         '', '(токен: ' + d.raw + ')');
  return L.join('\n');
}

// Пример строки для таблицы (Google Sheets): нишa | оборот | зоны | диагноз | токен | дата
function tokenToRow(token) {
  var d = decodeToken(token);
  if (!d) return ['—', '', '', '', token, new Date()];
  return [d.niche, d.turnover, d.zones.join('; '),
          d.diagnosis || 'острых зон нет', d.raw, new Date()];
}
```

Псевдокод для любого другого языка тот же: `split('_')` → проверить версию `d3`
и длину 6 → достать части по словарям выше → собрать текст/строку таблицы.

---

## 7. Поведение до доработки скрипта

Пока Apps Script не научится разбирать токен по этой спецификации, ссылка
`?start=` всё равно открывает @RidFinance_bot и клиент попадает в диалог —
**воронка не теряется**. Но разбор **развернётся в таблице только после
доработки скрипта**: до этого бот видит `/start` с непрочитанным параметром.
После доработки каждый токен превращается в строку таблицы и/или сообщение
Татьяне по шаблону из раздела 4.
